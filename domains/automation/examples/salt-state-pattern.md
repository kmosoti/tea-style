# Salt State Pattern

Keep pillar data declarative and keep state behavior focused.

## Shape
- pillar defines values
- state renders desired configuration
- orchestration handles ordering where needed

This preserves the separation between data and execution.
